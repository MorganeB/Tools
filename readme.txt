-- delete_files.bat
To do some cleanup on your computer based on file extension 
Typically launch as a scheduled task

-- download_files.bat
A function to download several files in a row with a timeout between each file for slow connection :) 
Change line "timeout 500" to set the timeout (0 -> n) 
To add a download, copy / paste one of the function command line 